<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>しりとりヘルパー</title>
</head>
<body>
    <h1>しりとりヘルパー</h1>
    <form action="/shiritori">
        <input type="text" name="word" placeholder="言葉を入力" required>
        <input type="submit" value="次の単語を提案">
    </form>
    <% if (word) { %>
        <p>入力した言葉: "<%= word %>"</p>
        <% if (gameOver) { %>
            <p style="color: red;"><%= nextWord %></p>
        <% } else if (valid) { %>
            <p>次の言葉の候補: "<%= nextWord %>"</p>
        <% } else { %>
            <p style="color: red;"><%= nextWord %></p>
        <% } %>
    <% } %>
</body>
</html>
